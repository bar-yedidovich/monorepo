auto-install-peers=true
strict-peer-dependencies=false

# Enforce the Node/pnpm versions declared in package.json and .node-version
engine-strict=true
node-version=24.0.0

# Pin exact versions when adding dependencies (no ^ or ~)
save-exact=true
