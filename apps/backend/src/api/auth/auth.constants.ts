import config from '../../config/config';

export const refreshJwtKey = config.refreshJwtKey;
export const accessJwtKey = config.accessJwtKey;
export const refreshExp = '30d';
export const accessExp = '14m';
