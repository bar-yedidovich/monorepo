/** Milliseconds in one second. */
export const MS = 1;

/** `1000 * MS` */
export const SECOND = 1000 * MS;

/** `60 * SECOND` */
export const MINUTE = 60 * SECOND;

/** `60 * MINUTE` — one hour in milliseconds. */
export const HOUR = 60 * MINUTE;
